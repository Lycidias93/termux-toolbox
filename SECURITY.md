# Security Policy

Do not commit secrets, private logs, real credentials, rclone configs, SSH keys, GitHub tokens, or private network inventory.

Before publishing, run:

```sh
bash ./verify/verify-termux-toolbox.sh
```
