#!/usr/bin/env bash
# AUTOCLIP_V95_LEGACY_CGRUN_CORE_COMPAT
set -euo pipefail

PREFIX_DIR="${PREFIX:-/data/data/com.termux/files/usr}"
CORE="$PREFIX_DIR/bin/cgrun-core-v95"

if [ ! -s "$CORE" ]; then
  printf 'FAIL: v9.5 cgrun core missing path=%s\n' "$CORE" >&2
  printf 'RESULT: CGRUN_CORE_COMPAT_FAILED outcome=runtime_missing workflow_exit_code=127\n' >&2
  exit 127
fi

exec bash "$CORE" "$@"
